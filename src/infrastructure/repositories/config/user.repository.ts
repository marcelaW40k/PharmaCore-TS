
import { Imanageable } from "../../../domain/models/Imanager/interface";
import { user } from "../../../domain/models/user";
import { getPoolConnection } from "../../../infrastructure/repositories/config/data.source";
import {  } from "module";

export class UserRepository implements Imanageable {
    create(body: object): Array<any> {
        

       
    }
    read(): Array<any> {
       
    }
    searcheById(id: number): Array<any> {
        
    }
    delet(id: number): Array<any> {
        return 
        
    }
    update(body: object): Array<any> {
        
    }
    

}